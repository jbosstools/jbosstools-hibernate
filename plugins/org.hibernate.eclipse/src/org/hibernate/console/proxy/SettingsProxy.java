package org.hibernate.console.proxy;

import org.hibernate.cfg.Settings;
import org.hibernate.console.spi.HibernateSettings;

public class SettingsProxy implements HibernateSettings {
	
	public SettingsProxy(Settings settings) {
	}

}
